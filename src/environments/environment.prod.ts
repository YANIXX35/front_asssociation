// URL du backend Django déployé sur Render (à mettre à jour une fois le service créé).
// Exemple une fois déployé : 'https://cesci-backend.onrender.com/api'
export const environment = {
  production: true,
  apiUrl: 'https://cesci-backend.onrender.com/api',
};
